server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Para Vue Router — redirige todo al index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Proxy al backend
    location /api {
        proxy_pass http://backend:3000;
    }
}